# Plan-doc audit — 2026-05-16

## Summary

- **18 markdown files audited** (3 index/log + 15 plan docs).
- **10 docs with stale phase status** (most contain "Designed.
  Not started." entries for work already shipped).
- **0 candidates for archival** — every plan doc still maps to
  an active workstream or remaining phases.
- **2 merge proposals** (ENGINE_PACK_SPLIT R4 → ENGINE_PACK_SHADERS;
  LIGHTING_ENTITIES_REFACTOR R1-R3 → ENGINE_PACK_SPLIT R1).
- **0 broken cross-links** — every `[NAME](./NAME.md)` resolves.
- **1 forward-reference to an unwritten doc** (`CONSOLE.md`,
  flagged in IDEAS.md only).
- **44 instances of pre-monorepo path notation** (`src/`,
  `resources/packs/`, `scripts/<X>.ts` root paths) — mostly inside
  the older five docs (WALL_OVERHAUL, LIGHTING_OVERHAUL,
  LIGHTING_ENTITIES_REFACTOR, ENGINE_PACK_SPLIT, MONOREPO_PLAN).
  Each is a candidate for a one-line replacement.
- **`SESSION_STATE.md` is materially out of date** (last touch
  2026-05-15; describes R3 follow-up as blocked; many shipped
  commits since).

---

## Per-doc findings

### docs/PLAN.md (250 lines)

**Status:** Mostly authoritative, mild drift in phase table.

**Issues:**
- §1 plan-doc index does not list the newer docs that landed
  after the table was written:
  `MATERIALS.md`, `ANIMATIONS.md`, `ANIMATION_EDITOR.md`,
  `AUDIO.md`, `EVENTS.md`, `EDITOR_IFRAME.md`. All have their
  own phase letters (M1–M5, A1–A4, AE1–AE3, Au1–Au4, Ev1–Ev3,
  I1–I3) and several have shipped phases.
- §7 phase status table omits the same rows. Specifically
  missing rows for:
  - MATERIALS M1+M2+M3+M5 shipped (commit `bdab12a`), M4 shipped
    (commit `be31fea`).
  - ANIMATIONS A1 shipped (commit `ab9dbee`).
  - ANIMATION_EDITOR AE1 shipped (commit `52d8e27`).
  - AUDIO Au1 shipped (commit `52d8e27`).
  - EVENTS Ev1 shipped (commit `52d8e27`).
  - EDITOR_IFRAME I1 shipped (commit `ab9dbee`).
  - PACK_CHAIN P1 shipped (commit `2edb94a`).
- §4 lists the ModAPI surface — out of date. Missing
  `api.audio`, `api.anim`, `api.events`, declared prefab
  registry, `api.world.findByName` (from commit `d8fcbe7`).
- §2 architecture diagram describes the editor as "scaffold."
  Commits `bdab12a` → `1961fd3` ship E1–E4 plus AE1, entities
  workflow tab, declarative prefabs, ProjectSettingsModal,
  dependency manager. The editor is materially functional now.
- §6 "operational gotchas" §3rd-bullet about
  `default-pack/character/player_idle.fbx` is no longer accurate
  — `ANIMATION_EDITOR.md` AE2 treats this FBX as a load-bearing
  acceptance fixture, not "unused content."

**Recommended action:**
- UPDATE §1 to add 6 missing plan-doc rows.
- UPDATE §7 phase-status table — add rows for MATERIALS,
  ANIMATIONS, ANIMATION_EDITOR, AUDIO, EVENTS, EDITOR_IFRAME,
  and mark phases shipped per commits.
- UPDATE §4 ModAPI list to reflect current surface (audio, anim,
  events, findByName, declarative-prefab registry).
- UPDATE §2 to flag editor as "functional, E1–E4 + AE1 shipped."
- UPDATE §6 FBX bullet to point at AE2 plan.

---

### docs/SESSION_STATE.md (223 lines)

**Status:** Substantially stale — last updated 2026-05-15, many
shipped commits since.

**Issues:**
- §1 declares "open R3 follow-up" but commit `aa069dd` shipped
  `api.ui` and moved `InventoryScreenSystem.tsx` /
  `SettingsScreenSystem.tsx` to the default pack (now at
  `packages/default-pack/scripts/systems/inventory-screen.tsx`
  and `settings-screen.tsx`).
- §2 queued tasks #157 + #158 listed as queued — both shipped.
- §3 "Open R3 follow-up" section entirely obsolete — same as
  above, the modals are now pack-side.
- §8 "Recommended next implementation work" recommends R4/S1
  and tile-presets T1 — both already shipped (commits `0a7fe16`,
  `c02d280`, `e786c3d`).
- §5 file-touched map describes work from 2 weeks ago; nothing
  about the 8 commits since.
- No mention at all of: AE1, AE2 (in progress per IDEAS.md #200),
  Au1, Ev1, A1, M1-M4, M5, P1, E1–E4, I1, declarative prefabs,
  dependency manager, project-settings modal.
- §4 design decisions ("Monaco over CodeMirror" etc.) still
  accurate but redundant with the relevant plan docs.

**Recommended action:**
- REWRITE this file entirely. The "Suggested new
  SESSION_STATE.md content" section in the Cross-cutting issues
  below sketches what it should now contain.

---

### docs/IDEAS.md (220 lines)

**Status:** Authoritative — newly created and accurate.

**Issues:**
- References `CONSOLE.md` plan that does not exist yet
  (acknowledged in entry — "CONSOLE.md not yet written").
- References `PACK_PROXY` task not yet created (acknowledged).
- Format and dates consistent.

**Recommended action:** None. Existing two "not yet" markers
are honestly self-flagged.

---

### docs/plans/MONOREPO_PLAN.md (220 lines)

**Status:** Authoritative-historical. Plan is fully landed
(PLAN.md row 1 marks it ✅). Doc is descriptive of a finished
refactor.

**Issues:**
- Contains 11 references to the pre-monorepo `src/` /
  `resources/packs/` / `scripts/` paths because it describes the
  before-and-after of the migration. These are intentional and
  shouldn't be rewritten.
- §7 "Docs reorg" describes the migration of plan docs into
  `docs/plans/` — done.
- §"Next steps" list points at R2/R3/M1, all of which the rest
  of the index now tracks.

**Recommended action:** None substantive — doc is a historical
record. Optionally add a one-line `**Status: Complete (workspace
landed in commit 539319e..).**` header so a future reader doesn't
mistake it for active work.

---

### docs/plans/ENGINE_PACK_SPLIT.md (298 lines)

**Status:** Partially stale — R4 stub has been superseded by
ENGINE_PACK_SHADERS.md.

**Issues:**
- §"Phased rollout" R4 section (lines 212–228) describes
  "Pack-shipped shaders with uniform auto-injection" in 17 lines
  but the actual implementation owner is now ENGINE_PACK_SHADERS.md
  (1482 lines, six phases S1–S6, with S1–S4 shipped).
  ENGINE_PACK_SHADERS.md §1 explicitly says it "supersedes the R4
  stub in ENGINE_PACK_SPLIT.md".
- §"R3" subtask list claims `api.input` / `api.events` /
  `api.modals` as "engine surfaces R3 needs" — `api.events`
  shipped in Ev1 (separate plan), `api.modals` shipped before.
- All path references are pre-monorepo (`src/Scene/`,
  `src/Prefabs/`, `resources/packs/default/scenes/`,
  `scripts/bake-lights.ts`, `src/main.ts`).
- §R5 ("pack override semantics") is acknowledged in PLAN.md
  row 2 as subsumed by PACK_CHAIN.md. The R5 section here is
  one-page redundant.

**Recommended action:**
- UPDATE §R4 to a one-paragraph redirect to ENGINE_PACK_SHADERS.md.
- UPDATE §R5 to a one-paragraph redirect to PACK_CHAIN.md.
- UPDATE all `src/...` and `resources/packs/...` paths to the
  monorepo equivalents (`packages/engine/src/...`,
  `packages/default-pack/...`, `apps/pack-builder/src/...`).
- Optionally mark R1+R2+R3 + R3 follow-up sections as ✅ at the
  top of each.

---

### docs/plans/WALL_OVERHAUL.md (581 lines)

**Status:** Partially stale — Phase 1 shipped, Phase 2+3 pending.

**Issues:**
- 17 pre-monorepo path references (`src/Scene.ts`,
  `src/Renderers/TwoDRenderer.ts`, `src/Libs/Raycast.ts`,
  `resources/packs/default/scenes/...`, `scripts/generate-scene.ts`).
- §13 "Bootstrap commands" calls `sed -n` against root `src/...`
  paths. Won't work today.
- No "Status" header — a reader can't tell from skimming what's
  done.
- Otherwise consistent with TILE_PRESETS cross-refs.

**Recommended action:**
- UPDATE all paths to the monorepo equivalents.
- UPDATE §13 bootstrap commands.
- Optionally add a "Phase status" table at the top.
- Otherwise content is accurate and remains the source-of-truth
  for Phase 2 + 3 work.

---

### docs/plans/LIGHTING_OVERHAUL.md (465 lines)

**Status:** Partially stale — Phases 1+2+4+5 shipped, Phase 3
pending.

**Issues:**
- 11 pre-monorepo path references (`src/Scene.ts`,
  `src/Renderers/...`, `src/Libs/Raycast.ts`,
  `scripts/bake-lights.ts`, `scripts/build-packs.ts`,
  `resources/packs/default/scenes/scene_heights_demo.json`).
- §"Phased delivery" lists Phase 1 + 2 + 3 + 4 + 5 + 6.
  PLAN.md says 1, 2, 4, 5 shipped — but this doc doesn't reflect
  that. Phases not annotated with status.
- §12 "Phase 1 starting point" bootstrap uses `cat
  src/Scene.ts` — broken.
- The bake migrated from `scripts/bake-lights.ts` (root) to
  `packages/engine/src/Lighting/Bake.ts` + called from
  `apps/pack-builder/src/build-packs.ts`. Doc says
  `scripts/bake-lights.ts`.

**Recommended action:**
- UPDATE all paths to monorepo equivalents (especially the bake
  location, which moved engine-side per commit `be31fea`).
- ANNOTATE phase headings with `✅ Shipped` / `⏳ Pending`.
- UPDATE §12 bootstrap commands.
- Optionally a short pointer at the top to
  LIGHTING_ENTITIES_REFACTOR.md (which is the lights-as-entities
  follow-up).

---

### docs/plans/LIGHTING_ENTITIES_REFACTOR.md (269 lines)

**Status:** Significantly stale — R1 (scene `entities[]` +
expansion) shipped as part of ENGINE_PACK_SPLIT R1 (commits
`bdab12a` / earlier). R2-R4 still pending.

**Issues:**
- §1 "Current state" still describes static lights as bare JSON
  records living only on `Scene.lights`. Today scenes have
  `entities[]` and a Light component on entity rows (R1 of this
  plan = R1 of ENGINE_PACK_SPLIT, both landed).
- 15 pre-monorepo paths (`src/Scene.ts:592`, `src/Game.ts:113-117`,
  `src/Components.ts`, `src/ECS/World.ts`, `src/ModAPI.ts`,
  `scripts/bake-lights.ts`, `resources/packs/default/...`).
- §"Phase R1" subtasks (lines 163–175) are largely shipped.
  Phases R2 (naming + `findByName`) is also shipped — commit
  `d8fcbe7` plus `api.world.findByName` exists. Confirm — see
  Cross-cutting findings below.
- R3 (Emissive + Anchored components) — NOT shipped. No
  `Anchored` or `Emissive` component exists at
  `packages/engine/src/Components/`.
- R4 (Path + AnimateEmissive systems + demo) — NOT shipped.
- "Bake-vs-runtime split" recommendation = pick option (1) is
  the path the bake already takes.

**Recommended action:**
- MERGE R1 status into ENGINE_PACK_SPLIT (or annotate as already
  done by ENGINE_PACK_SPLIT R1 here).
- ANNOTATE R2 as done (`findByName` confirmed in engine).
- UPDATE §1 "Current state" to today's reality.
- UPDATE all `src/...` paths to monorepo equivalents.
- KEEP R3 + R4 — these are the real remaining work and this doc
  is the source-of-truth.

---

### docs/plans/MULTIPLAYER_PLAN.md (274 lines)

**Status:** Authoritative + clean. No phases shipped (M1+ all
pending), so nothing to refute.

**Issues:**
- 3 pre-monorepo paths (`src/Components/`,
  `resources/packs/multiplayer/scripts/boot.js`,
  `src/Components.ts` line 188).
- §"Hard dependency on engine/pack split" recommends
  ENGINE_PACK_SPLIT R3 first — R3 has shipped, so this section
  could be tightened to say "R3 landed; M1 is unblocked."
- `api.events` was listed as a need; it has shipped (Ev1) which
  reduces M1's scope to net primitives only.
- §"What's missing today" lists a "dirty mechanism" and
  `NetworkId` / `Replicate` as pending — all still true.

**Recommended action:**
- UPDATE the 3 stale paths.
- UPDATE §"Hard dependency" to "ENGINE_PACK_SPLIT R3 + Ev1 +
  Au1 + A1 all landed; M1 is ready to start."

---

### docs/plans/PACK_CHAIN.md (502 lines)

**Status:** Partially stale — P1 shipped (commit `2edb94a`)
including `ChainResolver.ts`, multi-pack chains via `?pack=URL`
+ trust modal. P2–P5 pending.

**Issues:**
- §14 phase table lists P1 + P2 + P3 + P4 + P5 with no status
  annotations. P1 has shipped (per PLAN.md and the resolver file
  exists at `packages/engine/src/AssetPack/ChainResolver.ts`).
- §11 migration phases still listed as forward-looking — most
  of step 1 (schema additions) + step 2 (resolver) + step 3
  (multi-pack runtime) shipped.
- §13 "Files affected" lists `packages/engine/src/AssetPack/...`
  paths — those exist today: `ChainResolver.ts`, `semver.ts`,
  `loadAssetPack.ts`, `IdbAssetPack.ts`.
- Settings UI Packs panel (§9) is partly shipped — the editor's
  ProjectSettingsModal (commit `1961fd3`) ships a dependency
  manager UI. The game-side Settings tab isn't there yet.
- 8 pre-monorepo paths (mostly inside §13's file list).

**Recommended action:**
- ANNOTATE P1 as `✅ Shipped (commit 2edb94a)` in §14 + at
  relevant subsections (§4 resolver, §8 trust controls).
- UPDATE the §13 file list to note which files exist today.
- DEFER: the editor-side dependency manager could be cross-linked
  to PACK_CHAIN P2 as "partial — editor surface shipped, game
  surface pending."

---

### docs/plans/EDITOR.md (864 lines)

**Status:** Partially stale + overlap with EDITOR_IFRAME.md.

**Issues:**
- §2 architecture overview explicitly says **"Engine embed:
  in-process, not iframe."** This is the rejected approach. The
  iframe pivot shipped in commit `ab9dbee` (EDITOR_IFRAME.md I1).
  Lines 94–106 directly contradict the current architecture.
- §10 phase table lists E1–E5. PLAN.md says E1–E3 shipped, AE1
  + Entities mode shipped. Recent commits add E4 (bake move +
  entity/light) and project settings modal. The doc still
  describes E1–E5 as forward-looking with no annotations.
- §6.3 prefab placement was extended into declarative prefabs +
  Entities workflow tab (commit `d8fcbe7`) — that's a real
  change in approach that this doc doesn't reflect.
- §6.5 manifest editing — the manifest is no longer a tab in
  the Map mode; it's a dedicated Project Settings modal
  (IDEAS.md 2026-05-16 entry).
- §11 open question #1 "engine embed boundary" — RESOLVED
  by I1, but still listed as open.
- 5 pre-monorepo path references.

**Recommended action:**
- UPDATE §2 architecture section: replace the "in-process, not
  iframe" recommendation with "iframe runner; see EDITOR_IFRAME.md
  for the runtime architecture, this doc for editor-shell scope
  + phase plans."
- UPDATE §10 phase table: annotate E1–E4 + AE1 + Entities as
  shipped.
- UPDATE §6.3 to point at the declarative-prefab implementation
  (and IDEAS.md entry).
- UPDATE §6.5 to reference Project Settings modal split.
- UPDATE §11 open question #1 as resolved.
- DO NOT MERGE this doc with EDITOR_IFRAME.md — they describe
  different layers (this is the editor-shell scope; that is the
  iframe-runtime architecture). Keep both, but make the seam
  explicit in §2.

---

### docs/plans/EDITOR_IFRAME.md (614 lines)

**Status:** Authoritative for I1; I1 shipped, I2 + I3 pending.

**Issues:**
- §12 phase list — I1 is annotated as "this slice" but the doc
  was authored when I1 was the current PR. The wording should
  now read "I1 shipped (commit ab9dbee)."
- §14 acceptance criteria — once met, should be marked as such.
- §1 Goals — claims iframe-runner is opt-in via `?source=editor`
  — verify against current code. Commit `ab9dbee` did wire
  `editor-bridge.ts` and `boot-editor.ts` at `apps/game/src/`.
  Looks accurate.
- 12 pre-monorepo paths inside §5 + §8.

**Recommended action:**
- ANNOTATE I1 as shipped throughout.
- UPDATE pre-monorepo path references (mostly to
  `packages/engine/src/AssetPack/IdbAssetPack.ts` —  this file
  exists today, so the doc is half-converted already).
- Keep I2 + I3 sections as authoritative.

---

### docs/plans/TILE_PRESETS.md (1120 lines)

**Status:** Partially stale — T1+T2 shipped, T3+ pending.

**Issues:**
- 15 pre-monorepo path references (`src/Scene.ts`, etc. —
  although it consistently uses `packages/engine/src/` and
  `packages/default-pack/` in newer sections, the older sections
  haven't been refreshed).
- T1 + T2 (PresetResolver + JSONC + idMap scenes + build-merge
  dedupe) shipped per PLAN.md and confirmed by
  `packages/engine/src/AssetPack/PresetResolver.ts` existing.
  Phase table needs T1/T2 marked.
- T3 (validation), T4 (editor authoring), T5 (preset libraries)
  pending — accurate.
- T4 references editor-side preset library UI; EDITOR.md §6.1
  describes it but doesn't note that authoring is gated on T4
  here.

**Recommended action:**
- ANNOTATE T1 + T2 phases as shipped.
- Modest cleanup of any remaining `src/...` paths.
- Otherwise this doc is largely current and high-quality.

---

### docs/plans/ENGINE_PACK_SHADERS.md (1482 lines)

**Status:** Mostly authoritative. S1+S2+S3+S4 shipped (per
PLAN.md row 9 and commits `0a7fe16`, `c02d280`, `dd2063c`,
`be31fea`); S5 + S6 pending. Doc partially annotates this.

**Issues:**
- §14 phase table: S1 explicitly marked "DONE" + status
  "shipped (commit 5907c21)". S2/S3 should also be marked
  shipped (commit `c02d280` per PLAN.md table). S4 shipped
  (commit `dd2063c`). The doc doesn't yet reflect S2–S4
  status — at the level of the per-phase headings, they read
  forward-looking.
- §15 open question #1 (renderEngineWorld) is correctly marked
  RESOLVED. Good model for the others.
- Cross-refs to MATERIALS.md exist (correct).
- 12 pre-monorepo path references — most are within
  `packages/engine/src/Renderers/` (correct) but a few are
  bare `src/Renderers/` style. Minor.
- This doc is 1482 lines — borderline for a split (per audit
  brief's >1000 threshold) but the structure (3 modes × spec
  level × phase plan) is genuinely cross-cutting. Splitting
  would create more cross-references than it removes. Keep
  as-is.

**Recommended action:**
- ANNOTATE S2 + S3 + S4 as shipped (matching S1's annotation).
- UPDATE the ~5 lingering bare `src/...` path references.

---

### docs/plans/STORE.md (834 lines)

**Status:** Authoritative + clean. No phases shipped (all of
ST1–ST5 still pending). PLAN.md row 11 calls out the docs-site
iframe embed as "proof-of-concept" — accurate.

**Issues:**
- 2 path references in §"backend" — already correctly
  `apps/game` rooted. Clean.
- No phase status annotations needed yet (nothing shipped).
- §1 non-goals mentions a "future iteration" — fine for a
  forward-looking doc.

**Recommended action:** None. This is one of the cleanest docs
in the set. Revisit when ST1 starts.

---

### docs/plans/MATERIALS.md (771 lines)

**Status:** Stale phase table — M1+M2+M3+M5 shipped (commit
`bdab12a`), M4 shipped (commit `be31fea`). Doc still labels
M1 as "In progress (this PR)" and M2–M5 as "Designed. Not
started" / "Designed. Awaits PACK_CHAIN."

**Issues:**
- §13 phase table is materially wrong: PLAN.md table marks M1
  + M2 + M3 + M5 shipped via `bdab12a` and M4 shipped via
  `be31fea`. MATERIALS.md still shows them as designed.
- §15 "Implementation summary (M1)" — accurate for M1 but no
  parallel "Implementation summary (M2/M3/M4/M5)" sections.
- 14 path references — uses correct `packages/engine/src/...`
  style throughout. Good.
- No cross-doc redundancy beyond §3 hierarchy diagram which
  intentionally restates the ENGINE_PACK_SHADERS hook model.

**Recommended action:**
- UPDATE §13 phase table — mark M1+M2+M3+M5 shipped (commit
  `bdab12a`), M4 shipped (commit `be31fea`).
- Optionally add "Implementation summary" sections for M2–M5
  parallel to M1's.

---

### docs/plans/ANIMATIONS.md (1163 lines)

**Status:** Stale phase table — A1 shipped (commit `ab9dbee`).
Doc still says "Not started."

**Issues:**
- §13 phase table marks A1 as "Not started." A1 shipped in
  commit `ab9dbee` (PLAN.md row absent; see PLAN.md drift).
  AnimationSystem.ts + Animation component + api.anim all
  exist in current code.
- §14 open question #6 says "Hard dependency on api.events" —
  api.events has shipped (Ev1 / commit `52d8e27`). The
  conditional ("until api.events ships in MULTIPLAYER M1") is
  obsolete.
- A2 (mirror + 5-angle + crossfade + events) — the events
  integration is now unblocked.
- §1 description claims `character/player_idle.fbx` "stays
  unused" — but ANIMATION_EDITOR.md AE2 treats this file as the
  acceptance fixture. Internal inconsistency.
- 31 path references — all using correct
  `engine/src/...` or `default-pack/...` paths.

**Recommended action:**
- UPDATE §13 phase table: A1 shipped (commit `ab9dbee`).
- UPDATE §14 open question #6: api.events dependency satisfied.
- UPDATE §1 non-goals: cross-reference AE2 for the FBX path
  instead of saying it "stays unused."

---

### docs/plans/ANIMATION_EDITOR.md (1217 lines)

**Status:** Partially stale — AE1 shipped (commit `52d8e27`);
AE2 (FBX) in-progress per IDEAS.md #200. AE3 pending.

**Issues:**
- §9 phase descriptions don't annotate AE1 as shipped.
- §12 acceptance smoke test reads as forward-looking but AE1
  acceptance has been met.
- IDEAS.md 2026-05-16 mentions AE2 is "in progress (#200)" —
  this doc doesn't reflect that meta-state.
- 18 path references — all correct monorepo style.
- The very recent commit `1961fd3` (entities workflow tab, UI
  scale, scrollbar polish) is part of AE-adjacent editor work;
  not directly an AE phase.

**Recommended action:**
- ANNOTATE AE1 as shipped (commit `52d8e27`).
- ANNOTATE AE2 as in-progress.
- Tighten §12 wording so completed acceptance is past tense.

---

### docs/plans/AUDIO.md (1050 lines)

**Status:** Partially stale — Au1 shipped (commit `52d8e27`).
Doc still says "Not started."

**Issues:**
- §10 phase table marks Au1 as "Designed. Not started."
  PLAN.md doesn't have an AUDIO row, but commit `52d8e27`
  message includes "Au1 audio". The AudioRegistry exists at
  `packages/engine/src/ModAPI/AudioRegistry.ts`. ⚠️ Confirm:
  no `packages/engine/src/Audio/` directory exists — Au1 may
  have shipped a minimal/different surface vs the spec'd
  `engine/src/Audio/AudioBackend.ts`. Recommend the implementer
  verify Au1's shipped surface against §12 "Implementation
  summary."
- §11 open questions all still applicable.
- 21 path references — correct monorepo style throughout.

**Recommended action:**
- ANNOTATE Au1 as shipped (commit `52d8e27`).
- DOUBLE-CHECK §12 "Implementation summary" against the actual
  shipped code (no `engine/src/Audio/` folder — the API may have
  been folded into `engine/src/ModAPI/AudioRegistry.ts` instead).
  Either update §12 to match or flag the deviation.

---

### docs/plans/EVENTS.md (681 lines)

**Status:** Partially stale — Ev1 shipped (commit `52d8e27`).
Doc still says "Not started."

**Issues:**
- §11 phase table marks Ev1 as "Designed. Not started."
  EventsRegistry exists at
  `packages/engine/src/ModAPI/EventsRegistry.ts`; canonical
  events at `packages/engine/src/ModAPI/canonical-events.ts`.
- §11.1 Ev1 file map — all those files now exist.
- §12 open questions — most accepted with ✅. Good model.
- 10 path references — correct monorepo style throughout.

**Recommended action:**
- ANNOTATE Ev1 as shipped (commit `52d8e27`).
- ADD a brief §13 Implementation summary mirroring AUDIO §12 /
  ANIMATIONS §15 / MATERIALS §15 style.

---

## Cross-cutting issues

### PLAN.md phase-status table drift

The §7 phase status table in `docs/PLAN.md` is missing rows for
six entire plan docs that have shipped phases:

| Doc | Phases shipped | PLAN.md row exists? |
|---|---|---|
| MATERIALS.md | M1+M2+M3+M4+M5 | No |
| ANIMATIONS.md | A1 | No |
| ANIMATION_EDITOR.md | AE1 + Entities mode | Indirect (Editor row) |
| AUDIO.md | Au1 | No |
| EVENTS.md | Ev1 | No |
| EDITOR_IFRAME.md | I1 | No |

The §1 plan-doc index is missing those rows too. The Editor row
in §7 was last updated to mention E1–E3 + AE1 + Entities — but
E4 (bake-move + entity/light) and the dependency manager /
Project Settings modal aren't listed.

### Common stale-path patterns

About 75 places across plan docs still use the pre-monorepo
notation. Worst offenders by raw count:

- ANIMATIONS.md (31) — these are mostly inside §15
  "Implementation summary" file lists that read fine
  contextually but mix old + new style.
- LIGHTING_OVERHAUL.md (11) — bake script moved engine-side;
  doc still says `scripts/bake-lights.ts`.
- LIGHTING_ENTITIES_REFACTOR.md (15) — entire "Files to touch"
  section uses old paths.
- WALL_OVERHAUL.md (17) — same pattern.
- ENGINE_PACK_SPLIT.md (21) — descriptive of pre-monorepo state
  but mostly accurate as historical record.
- MONOREPO_PLAN.md (11) — historical, intentionally.

The path replacements are mostly mechanical:

| Old | New |
|---|---|
| `src/Components.ts` (root) | `packages/engine/src/Components/<X>.ts` |
| `src/Scene.ts` | `packages/engine/src/Scene.ts` |
| `src/Game.ts` | `packages/engine/src/Game.ts` |
| `src/Renderers/<X>.ts` | `packages/engine/src/Renderers/<X>.ts` |
| `src/Libs/Raycast.ts` | `packages/engine/src/Libs/Raycast.ts` |
| `src/Systems/` | `packages/engine/src/Systems/` (for
  engine-side systems; check if it moved to default-pack) |
| `src/Prefabs/` | gone — moved to
  `packages/default-pack/scripts/prefabs/` |
| `scripts/bake-lights.ts` | `packages/engine/src/Lighting/Bake.ts`
  (callsite: `apps/pack-builder/src/build-packs.ts`) |
| `scripts/build-packs.ts` | `apps/pack-builder/src/build-packs.ts` |
| `scripts/generate-scene.ts` | `apps/pack-builder/src/generate-scene.ts` |
| `resources/packs/default/` | `packages/default-pack/` |

### Shipped-but-unannotated work since SESSION_STATE.md

Since SESSION_STATE.md's 2026-05-15 timestamp, these commits
landed (in date order):

1. `0a7fe16` — R4 S1 pack shaders, Fumadocs, TILE_PRESETS draft.
2. `c02d280` — R4 S2+S3 (helper promotion + 38-hook shader hooks).
3. `e786c3d` — Tile presets T1+T2 (resolver + migration +
   build-merge).
4. `aa069dd` — `api.ui` modal-systems migration → pack
   (R3 follow-up done).
5. `bdab12a` — Full materials system (M1+M2+M3+M5) + editor E1
   + docs scene picker.
6. `dd2063c` — Editor E2 + R4 S4 (post-process passes).
7. `2edb94a` — PACK_CHAIN P1 + Editor E3 + ANIMATIONS/AUDIO/
   EVENTS plans + CRT smoke.
8. `be31fea` — M4 pack-chain shaders + Editor E4 (bake move +
   entity/light) + editor hosted on Pages.
9. `ab9dbee` — Editor iframe pivot + animation system A1 +
   scene4 anim demo + GitHub CORS rewrite.
10. `52d8e27` — Au1 audio + Ev1 events + AE1 animation editor.
11. `d8fcbe7` — Entities workflow tab + declarative prefabs +
    UI scale + scrollbar polish + iframe pivot dev-fix.
12. `1961fd3` — Project Settings modal + dependency manager +
    scrollbar consistency.

### Suggested new SESSION_STATE.md content

The current SESSION_STATE.md should be rewritten with content
roughly along these lines:

- **What's shipped recently:** the 12-commit list above.
- **What's in progress (per IDEAS.md):** AE2 (FBX importer,
  agent #200), #193 (semver enforcement, agent), #196
  (declarative prefabs hybrid, agent).
- **Open R3 follow-up section:** REMOVE (done).
- **Next-implementation recommendations:** MULTIPLAYER M1 is
  unblocked; LIGHTING_ENTITIES_REFACTOR R3/R4 are next-up;
  STORE ST1; WALL Phase 2; PACK_CHAIN P2 (Settings UI); TILE
  T3 (validation).
- **File-touched map:** all the editor views
  (`apps/editor/src/views/{HomeScreen,ProjectView,ProjectSettingsModal,EntitiesEditor,AnimationEditor,EditorViewport,GridEditor}.tsx`),
  the iframe bridge (`apps/game/src/editor-bridge.ts`,
  `apps/game/src/boot-editor.ts`), the audio + events + anim
  registries (`packages/engine/src/ModAPI/{AudioRegistry,EventsRegistry,AnimRegistry,canonical-events}.ts`),
  the shader hook system
  (`packages/engine/src/Renderers/{HookParser,HookPrelude,ShaderInjection,ShaderRoleRegistry,ShaderValidator,ShaderVariants,ShaderChainCascade,shaderHeaders,shaderHelpers,PostPassChain}.ts`),
  and IdbAssetPack + ChainResolver + semver + PresetResolver in
  `packages/engine/src/AssetPack/`.

---

## Recommended action plan (proposed order)

Each item is independently dispatchable. Listed roughly in
"highest signal-to-noise first" order.

1. **Rewrite `docs/SESSION_STATE.md`** from scratch with the
   shipped-commit list, open in-progress agents (#193 / #196 /
   #200), the closed R3 follow-up, and an updated file map.
   Biggest single-doc impact.

2. **Update `docs/PLAN.md`**:
   - Add 6 missing rows to §1 plan-doc index (MATERIALS,
     ANIMATIONS, ANIMATION_EDITOR, AUDIO, EVENTS, EDITOR_IFRAME).
   - Add rows to §7 phase-status table for each newly-shipped
     phase listed in the Cross-cutting findings.
   - Refresh §4 ModAPI surface (audio, anim, events, findByName,
     declarative-prefab registry).
   - Refresh §6 FBX bullet to point at AE2 instead of "unused."

3. **Annotate phase tables in the six "ship-but-doc-says-not"
   docs** — MATERIALS.md §13, ANIMATIONS.md §13, AUDIO.md §10,
   EVENTS.md §11, ANIMATION_EDITOR.md §9, EDITOR_IFRAME.md §12.
   Mark shipped phases with commit refs.

4. **Update `EDITOR.md` §2 to reflect the iframe pivot** —
   replace the "in-process, not iframe" architecture
   recommendation with a redirect to EDITOR_IFRAME.md, and
   resolve §11 open question #1.

5. **Refresh ENGINE_PACK_SPLIT.md §R4 and §R5** as one-paragraph
   redirects (to ENGINE_PACK_SHADERS.md and PACK_CHAIN.md
   respectively). Annotate R1–R3 as ✅ at the top.

6. **Refresh LIGHTING_ENTITIES_REFACTOR.md §1 + §R1 + §R2** to
   show what's now in code (the legacy `lights[]` → entities
   expansion ran, `findByName` exists). Keep R3 + R4 as the
   remaining unshipped work — this doc is the source-of-truth
   for those.

7. **Update LIGHTING_OVERHAUL.md** — fix path references
   (especially the bake-script move to
   `packages/engine/src/Lighting/Bake.ts`) and annotate phases
   1+2+4+5 as ✅.

8. **Update WALL_OVERHAUL.md path references and §13 bootstrap
   commands** (and optionally add a phase-status block at the
   top).

9. **Mark ENGINE_PACK_SHADERS.md S2 + S3 + S4 as shipped**
   parallel to S1's annotation.

10. **Mark TILE_PRESETS.md T1 + T2 as shipped**.

11. **Mark PACK_CHAIN.md P1 as shipped + note the editor-side
    dependency manager** under P2 as a partial-ship.

12. **Mark MULTIPLAYER_PLAN.md "Hard dependency" section** as
    satisfied — R3 + Ev1 are in.

13. **Audit AUDIO.md §12 against shipped code** — flag the
    apparent move from `engine/src/Audio/AudioBackend.ts` to
    `engine/src/ModAPI/AudioRegistry.ts` for the implementer.

14. **Optional, lower priority**: write `CONSOLE.md` skeleton
    (referenced in IDEAS.md #199); add `PACK_PROXY.md` skeleton
    (referenced in IDEAS.md 2026-05-15 entry). Both are
    explicitly captured-not-yet-written.

No doc is recommended for archival; the project's pace is
fast enough that yesterday's plan doc is tomorrow's reference
material.
