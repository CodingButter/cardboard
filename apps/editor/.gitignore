# Build output from `bun run build`. The `dist/` directory is wiped
# clean by the `clean` script before every build, so stale chunks
# can't pile up. Anything in `public/` is committed static assets
# (icons, manifest, etc.) and is NOT touched by the bundler — that's
# the pattern apps/game uses and the editor now matches.
dist/
