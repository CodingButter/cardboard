/**
 * Placeholder for cross-cutting types shared between the engine and
 * any future multiplayer server (Vec2, Component<T>, NetMessage, ...).
 *
 * Currently empty — `MULTIPLAYER_PLAN.md` M1 will populate it. Kept
 * as a workspace slot so subsequent moves are pure file relocations.
 */
export {};
