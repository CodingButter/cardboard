// PLACEHOLDER — pack-builder replaces this file with a bundled
// chart.js@4.4.0 ESM artefact at build time. Same bytes as the
// Performance Profiler pack's bundled chart.js (same entry,
// same version, same node_modules resolution). That byte-identity is
// the whole point of this pack: prove libraryCache's SHA-256 dedup
// shares one Blob URL across two packs.
//
// See `apps/pack-builder/src/build-packs.ts` library-bundling step.
export {};
